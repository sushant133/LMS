# OS / editor
.DS_Store
Thumbs.db
Desktop.ini
*.log
.idea/
.vscode/
*.swp
*~

# Accidental root installs (apps live in backend/ and frontend/)
/node_modules/
/package-lock.json

# Env secrets (also ignored per app)
.env
.env.*
!.env.example

# Firebase Admin / FCM service account keys — never commit
backend/secrets/
**/firebase-service-account.json
*-firebase-adminsdk-*.json
