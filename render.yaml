# Render Blueprint — backend API (Root Directory: backend in dashboard, or use rootDir below)
services:
  - type: web
    name: lms-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production