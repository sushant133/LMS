/**
 * Formerly showed a global “College Administrator — You have read-only access.”
 * banner for COLLEGE_VIEWER users. That message is no longer displayed.
 * Kept as a no-op so existing imports (AppLayout, AcademicCalendarPage) stay valid.
 */
export const ReadOnlyBanner = () => null;
