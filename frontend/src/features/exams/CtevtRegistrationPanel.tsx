import { CtevtFeePanel } from "features/exams/CtevtFeePanel";

/** CTEVT → Registration — registration fee Paid / Not Paid. */
export const CtevtRegistrationPanel = () => (
  <CtevtFeePanel feeKind="registration" />
);
