# Local dev — Vite proxies /api and /uploads to the backend (see vite.config.ts)
VITE_API_URL=/api

# --- Vercel production ---
# Set this in Vercel Project Settings > Environment Variables:
# VITE_API_URL=https://lms-s7tz.onrender.com/api